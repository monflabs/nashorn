# Functions and closures

Declarations, expressions and arrows; a closure keeping its counter alive; `arguments`, rest
parameters and default values; and `this`, which depends on how a function is called - plain
call, method call, `call`/`apply`/`bind` - except in an arrow, which inherits it from where it
was written. Run with **Log expression values** on to see what each call returns.
