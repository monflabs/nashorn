# Errors

`try`/`catch`/`finally`, the built-in error types, the stack an `Error` carries, and an error class of your own.

The last statement throws on purpose: an error that nothing catches ends the script, and the playground reports it with the script stack.
