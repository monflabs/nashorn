# Hello

`print` and `console.log` both write to the console below. `console.error` writes to the error stream, which the playground shows in red.

Edit the code: with **Auto-run** on, it runs again half a second after you stop typing. Tick **Log expression values** to see what every top-level statement evaluates to, printed beside its line.
