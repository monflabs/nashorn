# Trailing commas

ES2017 lets parameter lists and argument lists end with a comma, as array and object literals
always could - a grammar-only change that keeps one-line-per-item diffs clean. A comma with
nothing before it is still the syntax error it always was; the commented-out lines show both
refusals.
