# Destructuring

Unpacking arrays and objects in one pattern: positional and named targets, nesting, defaults for
missing pieces, renaming with `prop: name`, swapping without a temporary, and patterns in
function parameters. The engine desugars every pattern - declarations, assignments, parameters,
for-of heads - into the reads the specification prescribes, evaluation order included.
