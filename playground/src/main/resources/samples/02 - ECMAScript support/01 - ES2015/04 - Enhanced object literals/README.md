# Enhanced object literals

The ES2015 literal shorthands: `{ x, y }` for `{ x: x, y: y }`, method syntax without the
`function` keyword, computed property names in brackets, and `__proto__` in a literal (the last
is technically Annex B, and this engine has it on by default). Together they make object-building
code read like the object it builds.
