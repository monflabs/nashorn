# Java methods as functions

A Java method looked up on an object or a class is a function value: `call`, `apply` and `bind` work on it, and an overloaded method can be pinned to one signature with the `obj['name(type,type)']` form.
