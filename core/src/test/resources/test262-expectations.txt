# test262 ES2026 slice, engine defaults (optimistic types on): settled failures. None - every
# execution passes. The pessimistic run (-Dnashorn.test262.optimistic=false) has its own file,
# test262-expectations-pessimistic.txt; see doc/CONFORMANCE.md.
# Regenerate with -Dnashorn.test262.write.expectations=true. The build fails on any failure not
# listed here and on any listed test that starts passing, so conformance only moves forwards.
